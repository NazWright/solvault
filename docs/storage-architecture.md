# SolVault Storage System Architecture

## 🏗️ **System Overview Diagram**

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           SolVault Storage System v0.2.0                        │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐    ┌─────────────────┐    ┌─────────────────────────────────────┐
│                 │    │                 │    │                                     │
│   NFT Fetcher   │───▶│  Storage Layer  │───▶│        File System                  │
│   (v0.1.0)      │    │   (v0.2.0)      │    │                                     │
│                 │    │                 │    │                                     │
└─────────────────┘    └─────────────────┘    └─────────────────────────────────────┘
         │                       │                              │
         │                       │                              │
    ┌────▼────┐             ┌────▼────┐                    ┌────▼────┐
    │ Solana  │             │Storage  │                    │Directory│
    │   RPC   │             │Backend  │                    │Structure│
    │Connection│             │Interface│                    │         │
    └─────────┘             └─────────┘                    └─────────┘
```

## 🗂️ **Directory Structure Diagram**

```
SolVault Backup Directory
└── backup_root/
    └── wallets/
        └── {wallet_address}/               # e.g., h6VG3SKVfCjFavPC8r5ztnSCJFFPhm6yDmz...
            └── nfts/
                └── {mint_address}/         # e.g., ANg3FsUmzYDzvPffk9sv6EX15Jke13g...
                    ├── nft_data.json      # ← StoredNFT (main data + metadata)
                    ├── metadata.json      # ← Off-chain metadata (if available)
                    └── media/             # ← Future: images, videos, etc.
                        ├── image.png
                        ├── animation.mp4
                        └── thumbnail.jpg

Example Real Structure:
backup_root/
└── wallets/
    └── h6VG3SKVfCjFavPC8r5ztnSCJFFPhm6yDmzbZF8fEQP/
        └── nfts/
            └── ANg3FsUmzYDzvPffk9sv6EX15Jke13gPCtEBRQm2wL3/
                ├── nft_data.json       # Complete NFT record
                └── metadata.json       # JSON metadata from URI
```

## 🔄 **Data Flow Diagram**

```
Step 1: NFT Discovery
┌─────────────┐    fetch     ┌─────────────┐    parse     ┌─────────────┐
│   Solana    │─────────────▶│   Token     │─────────────▶│   NFT Info  │
│ Blockchain  │              │  Accounts   │              │   Object    │
└─────────────┘              └─────────────┘              └─────────────┘
                                    │
                                    ▼
                            ┌─────────────┐
                            │  Metadata   │
                            │    URI      │
                            └─────────────┘
                                    │
                                    ▼ HTTP fetch
                            ┌─────────────┐
                            │ Off-chain   │
                            │  Metadata   │
                            └─────────────┘

Step 2: Storage Processing
┌─────────────┐              ┌─────────────┐              ┌─────────────┐
│   NFT Info  │─────────────▶│  Storage    │─────────────▶│ File System │
│   Object    │   SaveNFT()  │  Backend    │  filesystem  │   Storage   │
└─────────────┘              └─────────────┘              └─────────────┘
                                    │
                                    ▼
                            ┌─────────────┐
                            │   Stored    │  ← Contains:
                            │    NFT      │     • Original NFT data
                            │   Object    │     • Storage metadata
                            └─────────────┘     • Checksums
                                               • Timestamps
```

## 🏛️ **Interface Architecture**

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              StorageBackend Interface                            │
├─────────────────────────────────────────────────────────────────────────────────┤
│  SaveNFT(ctx, nftInfo) error           │ Store NFT with metadata                │
│  GetNFT(ctx, wallet, mint) *StoredNFT  │ Retrieve specific NFT                  │
│  ListNFTs(ctx, wallet) []*StoredNFT    │ Get all NFTs for wallet                │
│  DeleteNFT(ctx, wallet, mint) error    │ Remove NFT from storage                │
│  Close() error                         │ Cleanup resources                      │
└─────────────────────────────────────────────────────────────────────────────────┘
                                         │
                                         ▼ implements
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              FileStorage Implementation                          │
├─────────────────────────────────────────────────────────────────────────────────┤
│  • Directory-based organization        │ • JSON serialization                   │
│  • Human-readable file structure       │ • SHA256 integrity checking            │
│  • Atomic operations per NFT           │ • Pretty-printed for debugging         │
│  • Graceful error handling             │ • Configurable permissions             │
└─────────────────────────────────────────────────────────────────────────────────┘

Future Implementations:
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Database      │    │   Cloud Storage │    │   Encrypted     │
│   Storage       │    │   (S3, etc.)    │    │   Storage       │
│                 │    │                 │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

## 📊 **Data Structure Relationships**

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                StoredNFT                                        │
├─────────────────────────────────────────────────────────────────────────────────┤
│  NFTInfo *fetcher.NFTInfo              │ ← Original fetched data                │
│  StoredAt    time.Time                 │ ← When saved to disk                   │
│  UpdatedAt   time.Time                 │ ← Last modification                    │
│  Version     int                       │ ← Data schema version                  │
│  Checksum    string                    │ ← SHA256 for integrity                 │
│  BackupPath  string                    │ ← Filesystem location                  │
│  Verified    bool                      │ ← Verification status                  │
│  LastCheck   time.Time                 │ ← Last verification time               │
└─────────────────────────────────────────────────────────────────────────────────┘
                              │
                              ▼ contains
┌─────────────────────────────────────────────────────────────────────────────────┐
│                               fetcher.NFTInfo                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│  MintAddress  solanago.PublicKey       │ ← Unique NFT identifier               │
│  TokenAccount solanago.PublicKey       │ ← Account holding the token            │
│  Owner        solanago.PublicKey       │ ← Current owner address                │
│  Metadata     *NFTMetadata             │ ← Off-chain metadata                   │
│  MetadataURI  string                   │ ← URL to metadata JSON                 │
│  Supply       uint64                   │ ← Token supply (1 for NFTs)            │
│  Decimals     uint8                    │ ← Decimal places (0 for NFTs)          │
│  FetchedAt    time.Time                │ ← When fetched from blockchain         │
└─────────────────────────────────────────────────────────────────────────────────┘
```

## 🔍 **Operation Flow Examples**

### Save NFT Operation
```
Input: NFTInfo from fetcher
         │
         ▼
1. Create StoredNFT wrapper
   ├── Add timestamps
   ├── Calculate checksum
   ├── Set version = 1
   └── Mark as unverified
         │
         ▼
2. Build file paths
   └── /wallets/{wallet}/nfts/{mint}/
         │
         ▼
3. Create directory structure
   └── os.MkdirAll() with permissions
         │
         ▼
4. Save files atomically
   ├── nft_data.json (StoredNFT)
   └── metadata.json (if available)
         │
         ▼
Output: Success or detailed error
```

### List NFTs Operation
```
Input: Wallet address
         │
         ▼
1. Build wallet path
   └── /wallets/{wallet}/nfts/
         │
         ▼
2. Scan directory tree
   └── filepath.Walk() to find nft_data.json
         │
         ▼
3. Load each NFT file
   ├── Parse JSON
   ├── Handle corrupt files gracefully
   └── Continue on errors
         │
         ▼
Output: Array of StoredNFT objects
```

## 🛡️ **Error Handling Strategy**

```
Error Categories:
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  File System    │    │  Data Integrity │    │  User Errors    │
│  Errors         │    │  Errors         │    │                 │
├─────────────────┤    ├─────────────────┤    ├─────────────────┤
│ • Permission    │    │ • Corrupt JSON  │    │ • Invalid keys  │
│ • Disk full     │    │ • Missing files │    │ • Not found     │
│ • Path invalid  │    │ • Bad checksums │    │ • Wrong format  │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         └───────────────────────┼───────────────────────┘
                                 │
                                 ▼
                    ┌─────────────────────┐
                    │  Graceful Handling  │
                    ├─────────────────────┤
                    │ • Wrap with context │
                    │ • Log warnings      │
                    │ • Continue on minor │
                    │ • Fail on critical  │
                    └─────────────────────┘
```

## 🎯 **Key Design Decisions**

1. **Interface-First Design**: Enables different storage backends (DB, cloud, encrypted)
2. **Human-Readable Format**: JSON files can be inspected and debugged easily  
3. **Atomic Directory Operations**: Each NFT gets isolated storage space
4. **Integrity Checking**: SHA256 checksums detect file corruption
5. **Metadata Separation**: Off-chain data stored separately for clarity
6. **Version Tracking**: Future-proofs against schema changes
7. **Graceful Degradation**: System continues working with partial failures

This storage system forms the foundation for backup, verification, and monitoring features in future releases!